{% extends 'base.html.twig' %}

{% block title %}VictoriousPuppy{% endblock %}

{% block body %}
    <h1>VictoriousPuppy</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ victorious_puppy.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ victorious_puppy.name }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ victorious_puppy.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_victorious_puppy_index') }}">back to list</a>

    <a href="{{ path('app_victorious_puppy_edit', {'id': victorious_puppy.id}) }}">edit</a>

    {{ include('victorious_puppy/_delete_form.html.twig') }}
{% endblock %}
