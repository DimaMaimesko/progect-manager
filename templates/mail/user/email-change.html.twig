{% extends '@email/default/notification/body.html.twig' %}

{% block content %}
    <p>Email change link:</p>
{% endblock %}

{% block action %}
    <spacer size="16"></spacer>
    {% set url = url('profile.email.confirm', {'token': token}) %}

    <p><a href="{{ url }}">{{ url }}</a></p>
{% endblock %}
